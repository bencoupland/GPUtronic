# results/

Gold gate outputs (`gputronic_gold_*`) and transfer-plant outputs (`gputronic_transfer_*`).
Large CSVs are xz-compressed.

Pre-Gold campaign folders live under:
`archive/pre-gold-2026-05/results/`

Note: `20260516_164554/` may remain here if created as root during an early sudo test.
Remove with: `sudo rm -rf results/20260516_164554`
