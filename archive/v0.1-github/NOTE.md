# v0.1 — first public tree

This directory is the GitHub `main` snapshot before Gold 1.0
(`main.cu`, GTX 1080 / Pascal, 10 Hz “RPM” ECU loop).

It stays in git history as the ancestor of Gold. Do not build or run
this as the project reference.

Current root: `make gold && ./build/gputronic_gold check`
